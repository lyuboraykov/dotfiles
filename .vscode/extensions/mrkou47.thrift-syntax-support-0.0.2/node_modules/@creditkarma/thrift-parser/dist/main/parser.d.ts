import { ThriftDocument, Token } from './types';
import { ErrorReporter } from './debugger';
export interface Parser {
    parse(): ThriftDocument;
    synchronize(): void;
}
export declare function createParser(tokens: Array<Token>, report?: ErrorReporter): Parser;
